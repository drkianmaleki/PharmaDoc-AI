# Python dependencies for PharmaDoc AI.
#
# Notes:
#   - ipython is required because evaluation.py uses IPython.display.display,
#     which is pre-installed in Colab but must be listed explicitly here.
#   - gradio is pinned to <6 because Gradio 6.x removed the `type=` argument
#     from gr.Chatbot() which the current UI relies on.
#   - tesseract-ocr is a system (apt) package, not pip -- see Dockerfile.

pymupdf>=1.24,<1.27
sentence-transformers>=3.0,<6
faiss-cpu>=1.8,<2
transformers>=4.45,<5
accelerate>=0.34,<2
gradio>=5.0,<6
pandas>=2.1,<3
openpyxl>=3.1,<4
python-docx>=1.1,<2
pillow>=10,<13
pytesseract>=0.3.10,<0.4
opencv-python-headless>=4.9,<5
openai>=1.40,<3
ipython>=8.0,<9
